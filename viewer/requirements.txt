# Tested combination (2026-07-12): numpy 2.4.3, scipy 1.14.1, open3d 0.19.0,
# opencv-python 5.0.0, pytest 8.3.3. scipy 1.14 warns about numpy >= 2.3 but
# all 46 nav tests pass on this pair; a fresh install resolves a newer
# mutually-compatible pair on its own.
numpy
scipy
open3d
opencv-python
pytest
